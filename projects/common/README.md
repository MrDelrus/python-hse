# Clients

This is an internal package with implementation of different clients (and potentially different code), which can be reused across different projects. For now the following code is implemented:

- Clients:
    - [OpenWeatherClient](common/clients/open_weather_client.py)
    - [OpenFoodClient](common/clients/open_food_client.py)
